// Album interfaces

export interface AlbumData {
    albumTitle: string;
    albumDescription: string;
    isProtected: boolean;
}

export interface ImageData {
    imageId: number;
    imagePath: string;
    imageTitle: string;
    imageDescription: string;
    imageDate: string;
}